-- This is a script property. We set it to a default value but any placed instance of the script can override this value, 
-- directly in the properties view in the editor.
go.property("speed", 360)

function init(self)
    msg.post("controller#script", "set_speed", { speed = self.speed })
end